<div class="container">
	<div class="row">
		<%= link_to 'Home', root_path %>
		<div class="col s8 offset-s1">
			<h2><%= @post.title %></h2>
			<h4> class="center-align"><%= @idea.description %></h4>
			<p><%= @post.current_user.name %></p>
		</div>
	</div>
</div>
		<hr>
		<p>Suggestions:</p>


<%= render(partial: 'form') %>

<%= link_to 'Back', ideas_path =>

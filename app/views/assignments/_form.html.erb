<%= form_for [@idea, @assignment] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.hidden_field :user_id, value: current_user.id %>

  <%= f.submit "Submit", class: "btn-large" %>
<% end %>
